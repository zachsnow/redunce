#!/bin/bash
set -e

# Install locally on MacOS.

# 1. Check if homebrew is installed; if not error and bail.

# 2. Check that the binary has been built; if not error and bail.

# 3. Copy the binary to ~/.local/bin and the library to somewhere that's NOT in Homebrew but is in the default DYLD_LIBRARY_PATH.


echo "Done."